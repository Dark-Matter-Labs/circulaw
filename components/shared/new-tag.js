// tags are clickable therefore must be button
// this is a temporary component to be implemented as part of the brand refresh

export default function Tag({ children }) {
  return <div>{children}</div>;
}
