import AboutSearch from '@/components/search/about-search';

export default function AboutSearchPage({ searchParams }) {
  return (
    <div>
      <AboutSearch searchParams={searchParams} />
    </div>
  );
}
