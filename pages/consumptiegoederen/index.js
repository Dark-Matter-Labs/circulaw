import Layout from "/components/layout";
import SectionTypes from "/components/section-types-list";

export default function Consumptiegoederen() {
  if (loading) {
    return <p>Loading...</p>;
  }
  return <Layout>Consumptiegoederen Placeholder</Layout>;
}
