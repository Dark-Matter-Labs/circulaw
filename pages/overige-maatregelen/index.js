import Layout from "/components/layout";

export default function OverigeMaatregelen() {
  if (loading) {
    return <p>Loading...</p>;
  }
  return <Layout>Overige Maatregelen Placeholder</Layout>;
}
