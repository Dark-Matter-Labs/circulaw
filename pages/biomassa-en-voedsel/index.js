import Layout from "/components/layout";

export default function BiomassaEnVoedsel() {
  if (loading) {
    return <p>Loading...</p>;
  }
  return <Layout>Biomassa En Voedsel Placeholder</Layout>;
}
